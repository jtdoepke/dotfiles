#!/usr/bin/env bash
# Path to pyenv
export PATH="$HOME/.pyenv/bin:$PATH"

# Local bin
export PATH="$HOME/.local/bin:$PATH"

# Golang
export GOPATH="$HOME"
export PATH="$GOPATH/bin:$PATH"

# Aliases
alias vim=nvim

